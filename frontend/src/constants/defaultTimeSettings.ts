export const defaultFromDate = new Date(2000, 1, 1);

export const defaultToDate = new Date(2000, 1, 2);

export const defaultReloadTime = 5;

export const defaultEFCache = true;

export const defaultRedisCache = false;
